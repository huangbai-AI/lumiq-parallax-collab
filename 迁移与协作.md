# 迁移与协作

## 完整恢复

先安装Git LFS与Node.js（建议22 LTS），然后执行：

```bash
git lfs install
git clone https://github.com/huangbai-AI/lumiq-parallax-collab.git
cd lumiq-parallax-collab
git switch main
git lfs pull
git lfs fsck
cd related/website
npm ci
npm run build
npm run start -- --port 4211
```

浏览器打开 `http://localhost:4211/en`。日常开发用 `npm run dev -- --port 4211`。检查命令：`npm test`、`npm run typecheck`、`npm run lint`、`npm run check:i18n`。

## 素材与部署

图片、PSD、视频、文档和三维文件使用Git LFS。GitHub普通ZIP下载可能只有资源指针，完整交接请按上面的方式克隆。网页运行只依赖 `related/website/`；`output/` 保存母版、生成记录和检查资料。

Vercel项目为 `lumiq-home-pearl-preview`，发布目录为 `related/website/`，网址 https://lumiq-home-pearl-preview.vercel.app 。本地Vercel绑定、账号登录和密钥不随仓库提交；新电脑需要使用有权限的账号重新绑定。

候补名单环境变量以 `related/website/.env.example` 为模板；配置真实存储和数据表后再验收报名功能。禁止把真实值提交Git。

## 后续协作

从最新 `main` 开 `codex/` 分支，分段提交并合回主分支。固定交接版本见 `handoff-2026-09-07`。导入前来源仍保留在 `related/README.md`，不代表当前代码版本。
