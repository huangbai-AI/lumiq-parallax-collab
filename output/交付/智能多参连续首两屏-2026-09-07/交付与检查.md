# H3 智能多参连续首两屏

按用户最新要求，使用 H3「智能多参」依次引用无字起始图、用户确认首屏、品牌第二屏，一次生成约 7.3 秒的连续影片。网页现只使用一个视频元素和一个文件，替代此前入场与过渡两条视频的切换。分支仍为 `codex/lumiq-floors-1-2`，从 `f8a8135` 继续修改，未推送、部署或合并。

## 当前观看流程

1. LUMIQ 字母由 AI 影片逐个出现。网页自动播放原片 0–3.5 秒，以 1.6 倍速度展示，实际入场约 2.19 秒。该结束点来自逐帧观察，包含 H3 自带的完整首屏停留。
2. 用户向下滚动后，同一个播放器从 3.5 秒继续到约 7.247 秒，产品退出并显露品牌第二屏。反向滚动回到完整首屏，不重播字母入场。
3. 视频文件保留模型生成的整条时间线，未裁切、拼接、交叉淡化或重新合成字母。导航、正文和卡片保持真实网页。
4. 小屏、短窗口、粗指针、减少动态和 `?opening=code` 沿用旧布局，不加载视频。超宽屏两侧继续渐变延伸，正文和卡片随有效画幅排布。第二屏人物位于影片内，不叠放旧角色图。

本说明取代旧交付中“必须保留两段视频”的要求；旧文件保留作历史，不再作为当前网页来源。

## 素材与复现

- 提示词、实际生成模式与三图顺序：`output/libtv/opening-mixed-h3-2026-09-07/`。
- 原始成片：该目录 `generated/智能多参首屏入场到品牌H3-20260907.mp4`。
- 网页素材：`related/website/public/assets/home-video/opening-mixed-h3-20260907.mp4`，2560×1440，24 帧，7.291667 秒，H.264、无音轨、快速起播，11,435,690 字节。
- 导出：在 `related/website/` 执行 `node scripts/prepare-opening-mixed-assets.mjs`；只重新编码，不重新生成 AI 内容。
- 本目录 `H3智能多参连续原片.mp4` 为去音轨后的同一完整影片；`网页完整演示.mp4` 展示实际网页入场、滚动和第二屏。

## 检查结果

- 构建、类型及静态检查通过，87 个页面生成成功。
- 1440×900 下，滚动 0／180／360／720／1080／1440 像素时，同一视频分别约为 3.506／3.964／4.433／5.365／6.302／7.239 秒。反向回到 720／0 像素时为 5.388／3.515 秒，没有重播入场。
- 4.5 秒连续滚动并录屏时，收到 120 次画面呈现回调，最大间隔约 53 毫秒；最终停在 7.244 秒。这是本机 Chromium 的实测，不外推到所有设备。
- 上述全部采样均为同一个视频元素、同一个地址、可见状态，没有替换播放器或切换源。页面没有脚本错误。
- 3808×1862、2560×1080、1280×720、390×844 和 1440×650 均无横向溢出。390 宽与短窗口视频地址为空；减少动态与旧代码模式地址也为空。
- 视频请求失败时，第二屏备用静态图正常可见；`#ola` 直达第二屏时跳到原片约 7.247 秒。
- 第三屏保留五张产品卡片，滚动中点横移约 -608 像素、楼层仍停留顶部，原交互正常。
- 首、中、尾、入场分界和浏览器截图分别在素材 `review/` 与 `output/playwright/opening-mixed-h3-2026-09-07/`。
- 本轮为电脑 Chromium 和模拟尺寸检查，未扩大为 Safari 或手机实机验收。模型仍存在设备内原画女孩到品牌立体女孩的画风变化，未声称三维模型完全一致。

本地预览：`http://127.0.0.1:4201/en?v=mixed3`。刷新后看约 2.2 秒入场，再向下滚动进入第二屏。主项目 4198、4193 未修改。
