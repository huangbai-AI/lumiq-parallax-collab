# LumiQ 客户审阅版 · 2026-09-07

本轮在 `codex/lumiq-girl-position-smooth-v2` 分支分批修改，每批独立提交。网站代码位于 `related/website/`。

## 打开与检查

本机已构建预览：<http://127.0.0.1:4211/en>。繁中和日文入口分别为 `/zh-hant`、`/ja`。这个地址只能在本机打开，尚未发布外网。

重新打开：在网站目录执行 `npm ci`、`npm run build`、`npm run start -- --port 4211`。素材由 Git LFS 管理，迁移后先执行 `git lfs pull`。

## 已完成

1. 首屏左右渐变仅在宽度至少 1600 像素且画幅达到 2:1 时启用。普通电脑不再出现两侧白边；超宽屏背景自然延展。
2. 首两屏继续用同一个 H3 连续视频。女孩最终位置约在横向 34%，左侧文字和右侧三张卡留出间距。约两秒完成 LUMIQ 入场，随后滚动进入第二屏。
3. 视频保留 2560 × 1440、24 帧、7.291667 秒。逐帧独立编码支持前后滚动；卡片依次上浮，最后上下边对齐。旧影片仍保留，可回退。
4. 首页及其他页面共用一级标题、二级标题、三级标题、正文与按钮尺寸。保留原页面字体风格，修正手机上过大的产品标题和过小的说明文字。
5. 手机端按内容自然向下阅读；产品横向列表可滑动；房间场景下方提供可读的产品说明；家庭三段照片支持点选。
6. 其他页面首屏文字立即显示，不再依赖淡入后才能阅读；长段落滚入时不会因显示比例不足而一直隐藏。登记请求超时后可恢复重试。

## 字号规范

| 层级 | 电脑 | 手机 |
| --- | --- | --- |
| 一级标题 | 44–78 px | 40–64 px |
| 二级标题 | 36–52 px | 32–40 px |
| 三级标题 | 20–26 px | 22 px |
| 正文 | 16–18 px | 16 px |
| 卡片说明 | 14 px | 14 px |
| 按钮、文字入口 | 14 px | 14 px |

尺寸随可用宽度变化，同一宽度使用同一规则。统一定义在 `related/website/app/site-typography.css`。

## 检查记录

- 构建成功，87 个路由输出；类型检查、代码检查通过。
- 16 项单元检查通过。
- 在已构建版本运行原有浏览器流程：15 项通过；11 项按电脑/手机职责分工跳过，未有失败。
- 913 个翻译键在三种语言中完整对应。
- 手机与电脑整页走查和最后的截图记录见同目录的 `走查与待办.md`。

## 交付边界

这是供客户审阅的网站版本。人物边缘精修、真实名单数据库，以及媒体、商业和法律文案仍有待确认项，详见 `走查与待办.md`。本轮未推送、未部署生产环境。
