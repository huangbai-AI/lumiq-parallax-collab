# 新 Codex 接力说明

先读根目录README、当前进度、迁移与协作，以及 `output/交付/GitHub交接-2026-09-07/交接说明.md` 和同目录素材索引。

1. 从 `main` 或 `handoff-2026-09-07` 开新的 `codex/` 分支。真实网站在 `related/website/`，不需重新寻找源代码。
2. 电脑首两屏使用同一个连续视频；手机采用独立高清图片布局。不要恢复旧手机产品拼图、人物黑边素材或早期仅尾帧影片。
3. 当前网页资源路径保持原位，高清母版与提示词按素材索引取用。Git LFS必须完整拉取，不能把指针文件当图片使用。
4. 分段修改、检查并提交；不提交登录信息、缓存、真实环境变量。生产名单库未配置，不可声称表单可真实存储。
5. 历史交付文件里的旧提交号，可在 `大文件迁移提交对应.csv` 查到新提交号；网站文件内容不因迁移而变化。
6. 其他工作树如有独立未提交工作应保留，不以清理分支为由覆盖。
